import * as KSceletonStories from '../__stories__/KSceleton.stories';
import { composeStories } from '@storybook/react-vite';

export const KSceletonComposedStories = composeStories(KSceletonStories);
